# Dependency Discipline Pack

-   Prefer latest versions without Critical/High CVEs
-   Avoid dependencies for \<300 LOC functionality
-   Justify every dependency
-   Prefer native implementations where feasible
